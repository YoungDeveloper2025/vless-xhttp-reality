# Shared default settings for install.sh and install-auto.sh.
# Edit only this file to change the defaults used by both installers.
DEFAULT_SNI="amp-api-edge.apps.apple.com"
DEFAULT_PORT="8443"
DEFAULT_FINGERPRINT="edge"
DEFAULT_PATH="xk5mv5vF7q?ed=2560"
DEFAULT_MODE="auto"
DEFAULT_REMARK="VLESS-XHTTP-Reality"
OUTPUT_TITLE="VLESS + XHTTP + REALITY"
REALITY_TARGET_PORT="443"
XRAY_LOG_LEVEL="warning"
XRAY_LISTEN_ADDRESS="0.0.0.0"
XRAY_INSTALL_CHANNEL="stable"
XRAY_INSTALL_SCRIPT_URL="https://github.com/XTLS/Xray-install/raw/main/install-release.sh"
XRAY_CONFIG_FILE="/usr/local/etc/xray/config.json"
APT_LOCK_TIMEOUT="300"
